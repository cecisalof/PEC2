/**
 * @class Model
 *
 * Manages the data of the application.
 */

class Expense {
    constructor({ text, amount }) {
        console.log(amount);
        console.log(text);
        this.text = text,
        this.amount = amount 

        console.log(this);
    }
  }
  